<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>build.krema.demo</groupId>
    <artifactId>krema-angular-app</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Krema Angular App</name>
    <description>Desktop application with Angular frontend and Krema backend</description>

    <properties>
        <maven.compiler.release>25</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<krema.version>0.3.2</krema.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>build.krema</groupId>
            <artifactId>krema-core</artifactId>
            <version>${krema.version}</version>
        </dependency>
        <dependency>
            <groupId>build.krema</groupId>
            <artifactId>krema-plugin-positioner</artifactId>
            <version>${krema.version}</version>
        </dependency>
    </dependencies>

    <profiles>
        <profile>
            <id>dev</id>
            <properties>
                <krema.args>--dev</krema.args>
            </properties>
        </profile>
        <profile>
            <id>prod</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <krema.args></krema.args>
            </properties>
        </profile>
    </profiles>

    <build>
        <sourceDirectory>src-java</sourceDirectory>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <release>25</release>
                    <parameters>true</parameters>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>build.krema</groupId>
                            <artifactId>krema-processor</artifactId>
                            <version>${krema.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <executable>java</executable>
                    <arguments>
                        <argument>-XstartOnFirstThread</argument>
                        <argument>--enable-native-access=ALL-UNNAMED</argument>
                        <argument>-Djava.library.path=../../krema/krema-core/lib</argument>
                        <argument>-classpath</argument>
                        <classpath/>
                        <argument>build.krema.demo.app.Main</argument>
                        <argument>${krema.args}</argument>
                    </arguments>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.4.1</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>build.krema.demo.app.Main</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
